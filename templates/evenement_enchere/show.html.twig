{% extends 'base.html.twig' %}

{% block title %}EvenementEnchere{% endblock %}

{% block body %}
    <h1>EvenementEnchere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evenement_enchere.id }}</td>
            </tr>
            <tr>
                <th>EvenementEnchere</th>
                <td>{{ evenement_enchere.EvenementEnchere }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_evenement_enchere_index') }}">back to list</a>

    <a href="{{ path('app_evenement_enchere_edit', {'id': evenement_enchere.id}) }}">edit</a>

    {{ include('evenement_enchere/_delete_form.html.twig') }}
{% endblock %}
