<form method="post" action="{{ path('app_evenement_enchere_delete', {'id': evenement_enchere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement_enchere.id) }}">
    <button class="btn">Delete</button>
</form>
