{% extends 'base.html.twig' %}

{% block title %}Modifier l’évènement d’enchères{% endblock %}

{% block body %}
  <main class="bg-black text-white py-5" style="min-height:100vh;">
    <div class="container" style="max-width:800px;">
      <div class="d-flex justify-content-between align-items-center mb-3">
        <h1 class="h3 text-accent m-0">Modifier l’évènement</h1>
        <a href="{{ path('app_evenement_enchere_index') }}" class="btn btn-outline-light">← Retour</a>
      </div>
      <div class="card bg-dark border-light-subtle">
        <div class="card-body">
          {{ include('evenement_enchere/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
          <div class="mt-2">
            {{ include('evenement_enchere/_delete_form.html.twig') }}
          </div>
        </div>
      </div>
    </div>
  </main>
{% endblock %}
