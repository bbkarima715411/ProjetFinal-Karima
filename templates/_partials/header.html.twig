<header class="border-b bg-white/90 backdrop-blur supports-[backdrop-filter]:bg-white/70 sticky top-0 z-40">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="flex h-16 items-center justify-between">
      <!-- Logo -->
      <a href="{{ path('app_accueil') }}" class="font-bold text-lg tracking-tight text-gray-800">🏷️ Enchères</a>

      <!-- Mobile burger -->
      <details class="lg:hidden">
        <summary class="list-none inline-flex items-center gap-2 rounded-md border px-3 py-2 text-sm cursor-pointer select-none">
          <span>Menu</span>
          <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
          </svg>
        </summary>

        <!-- Mobile panel -->
        <nav class="mt-3 space-y-2 border-t pt-3">
          <a class="block rounded-md px-3 py-2 hover:bg-gray-100" href="{{ path('app_accueil') }}">Accueil</a>
          <a class="block rounded-md px-3 py-2 hover:bg-gray-100" href="{{ path('app_lot_index') }}">Objets en cours</a>
          <a class="block rounded-md px-3 py-2 hover:bg-gray-100" href="{{ path('app_contact') }}">Contact</a>

          {% if app.user %}
            <a class="block rounded-md px-3 py-2 hover:bg-gray-100" href="{{ path('app_profil') }}">Mon compte</a>
            <a class="block rounded-md px-3 py-2 hover:bg-gray-100" href="{{ path('app_logout') }}">Déconnexion</a>
          {% else %}
            <a class="block rounded-md px-3 py-2 hover:bg-gray-100" href="{{ path('app_login') }}">Connexion</a>
            <a class="block rounded-md px-3 py-2 hover:bg-gray-100" href="{{ path('app_register') }}">Créer un compte</a>
          {% endif %}
        </nav>
      </details>

      <!-- Desktop nav -->
      <nav class="hidden lg:flex items-center gap-2">
        <a class="rounded-md px-3 py-2 text-sm hover:bg-gray-100" href="{{ path('app_accueil') }}">Accueil</a>
        <a class="rounded-md px-3 py-2 text-sm hover:bg-gray-100" href="{{ path('app_lot_index') }}">Objets en cours</a>
        <a class="rounded-md px-3 py-2 text-sm hover:bg-gray-100" href="{{ path('app_contact') }}">Contact</a>

        {% if app.user %}
          <details class="relative group">
            <summary class="list-none rounded-md px-3 py-2 text-sm hover:bg-gray-100 cursor-pointer">Mon compte ▾</summary>
            <div class="absolute right-0 mt-2 w-48 rounded-xl border bg-white p-2 shadow-xl hidden group-open:block">
              <a class="block rounded px-3 py-2 text-sm hover:bg-gray-100" href="{{ path('app_profil') }}">Profil</a>
              <a class="block rounded px-3 py-2 text-sm hover:bg-gray-100" href="{{ path('app_mes_encheres') }}">Mes enchères</a>
              <a class="block rounded px-3 py-2 text-sm hover:bg-gray-100" href="{{ path('app_mes_favoris') }}">Mes favoris</a>
              <a class="block rounded px-3 py-2 text-sm hover:bg-gray-100" href="{{ path('app_mes_factures') }}">Mes factures</a>
              <div class="my-1 h-px bg-gray-200"></div>
              <a class="block rounded px-3 py-2 text-sm hover:bg-gray-100" href="{{ path('app_logout') }}">Déconnexion</a>
            </div>
          </details>
        {% else %}
          <a class="rounded-md px-3 py-2 text-sm hover:bg-gray-100" href="{{ path('app_login') }}">Connexion</a>
          <a class="rounded-md px-3 py-2 text-sm hover:bg-gray-100" href="{{ path('app_register') }}">Créer un compte</a>
        {% endif %}
      </nav>
    </div>
  </div>
</header>
