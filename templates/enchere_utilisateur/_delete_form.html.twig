<form method="post" action="{{ path('app_enchere_utilisateur_delete', {'id': enchere_utilisateur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enchere_utilisateur.id) }}">
    <button class="btn">Delete</button>
</form>
